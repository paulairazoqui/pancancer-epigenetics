# src

Python source code is organized with roadmap v3.0 phase-mirrored folders plus shared utilities.

Current implementation note: most phase-mirrored folders contain only `.gitkeep` placeholders. The active phase-specific source file remains `src/phase1_data_acquisition_and_auditing/download_public_datasets.py`; it is intentionally kept in place to avoid broad reference churn during this cleanup pass.
